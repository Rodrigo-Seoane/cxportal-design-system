---
title: Stats Cards
description: Compact KPI cards for executive dashboards. Combine a metric value, category icon, and color-coded trend indicator so stakeholders can assess performance at a glance.
figmaNodeId: 814-10561
figmaUpdated: Apr 13, 2026
status: stable
---

## Overview

Stats Cards (also called Data Cards) are compact dashboard components that display key performance indicators at a glance. Each card pairs a large metric value with a category icon, a label, and a trend indicator showing directional change against a reference period — enabling executives to assess business performance in under two seconds without reading detailed text.

## When to use

- Executive dashboards requiring quick KPI assessment
- Campaign performance overviews (delivery rates, response rates, message volumes)
- Resource utilization summaries (credits, capacity)
- Trend comparisons — week-over-week, month-over-month, period-over-period
- High-level status indicators for stakeholders

**Do not use Stats Cards for:**
- Detailed data analysis requiring drill-down — use Tables instead
- Time-series visualization — use Charts instead
- Multi-dimensional comparisons — use Data Tables instead
- Form inputs or data entry — use Form Fields instead

## Anatomy

A Stats Card is composed of four parts:

- **Category icon** — 36×36px (regular, 24px inner glyph) or 24×24px (small, 16px inner glyph) rounded square, solid green fill. The icon communicates what metric category this card belongs to (e.g. PaperPlaneTilt for SMS, Microphone for Voice Duration).
- **Trend indicator** — A directional arrow (8px) followed by a percentage and period label. Arrow color signals direction: green for increase, red for decrease.
- **Metric label** — The name of the KPI (10px, semibold). Identifies the card when multiple are shown side-by-side.
- **Value** — The primary metric (24px / Heading H2 for regular, 18px / Heading H4 for small). The largest element — intended for fast scanning.

## Variants

### Surface

<Table size="compact" className="mb-6">
  <TableHeader>
    <tr>
      <TableHead>Surface</TableHead>
      <TableHead>Background</TableHead>
      <TableHead>Use case</TableHead>
    </tr>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell><strong>White</strong></TableCell>
      <TableCell><code>#ffffff</code></TableCell>
      <TableCell>Standard cards on light section backgrounds</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><strong>Blue</strong></TableCell>
      <TableCell><code>#f8f8f8</code></TableCell>
      <TableCell>Highlighted or selected cards; subtle emphasis. Despite the name, this surface renders as the same gray used for the table zebra-stripe row.</TableCell>
    </TableRow>
  </TableBody>
</Table>

Both surfaces share the same `#cdcdcd` border (`--border-color-form-fields-default`) and `8px` border radius. Previously the card used `--neutral-100` (`#efefef`) for the border — a different, lighter gray that didn't match Figma's actual component.

### Size

<Table size="compact" className="mb-6">
  <TableHeader>
    <tr>
      <TableHead>Size</TableHead>
      <TableHead>Width</TableHead>
      <TableHead>Icon</TableHead>
      <TableHead>Value text</TableHead>
      <TableHead>Padding / gap</TableHead>
    </tr>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell><strong>Regular</strong></TableCell>
      <TableCell>180px, content-driven height (98px min)</TableCell>
      <TableCell>36×36px, 24px inner glyph</TableCell>
      <TableCell>24px / Heading H2</TableCell>
      <TableCell>12px padding, 12px gap</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><strong>Small</strong></TableCell>
      <TableCell>172px, fixed 80px height</TableCell>
      <TableCell>24×24px, 16px inner glyph</TableCell>
      <TableCell>18px / Heading H4</TableCell>
      <TableCell>8px padding, no gap (content is split via <code>justify-content: space-between</code> across the fixed height instead)</TableCell>
    </TableRow>
  </TableBody>
</Table>

Regular and Small aren't just a typography/icon scale-down — they use different layout strategies. Regular's height grows with content and spaces its rows with an explicit 12px gap; Small locks the height to 80px and relies on `justify-content: space-between` to push the icon row and the label/value block apart. The previous implementation used one fixed `8px 12px` padding and one fixed `8px` gap for both sizes, and a `82px` min-height for Small instead of Figma's `80px` — all corrected.

### Trend type

<Table size="compact" className="mb-6">
  <TableHeader>
    <tr>
      <TableHead>Type</TableHead>
      <TableHead>Arrow</TableHead>
      <TableHead>Color</TableHead>
      <TableHead>Meaning</TableHead>
    </tr>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell><strong>Increase</strong></TableCell>
      <TableCell>ArrowFatLineUp ↑</TableCell>
      <TableCell>Success green</TableCell>
      <TableCell>Positive trend vs reference period</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><strong>Decrease</strong></TableCell>
      <TableCell>ArrowFatLineDown ↓</TableCell>
      <TableCell>Error red (<code>--error-default</code>, <code>#ef2056</code>)</TableCell>
      <TableCell>Negative trend vs reference period. Previously used <code>--text-destructive</code>, which resolves to a darker error step (<code>#ab0c36</code>) that doesn't match Figma's <code>text/error</code> value.</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><strong>Neutral</strong></TableCell>
      <TableCell>ArrowFatLineUp ↑</TableCell>
      <TableCell>Secondary gray</TableCell>
      <TableCell>No significant change, or direction unknown</TableCell>
    </TableRow>
  </TableBody>
</Table>

### Category icons

<Table size="compact" className="mb-6">
  <TableHeader>
    <tr>
      <TableHead>Icon key</TableHead>
      <TableHead>Use case</TableHead>
    </tr>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell><code>sms-sent</code></TableCell>
      <TableCell>Message volume metrics</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code>voice-duration</code></TableCell>
      <TableCell>Call time metrics</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code>delivery-rate</code></TableCell>
      <TableCell>Successful deliveries</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code>open-rate</code></TableCell>
      <TableCell>Email / message opens</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code>response-rate</code></TableCell>
      <TableCell>User engagement</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code>opt-out</code></TableCell>
      <TableCell>Unsubscribe tracking</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code>voice-survey</code></TableCell>
      <TableCell>Survey response metrics</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code>voice-notification</code></TableCell>
      <TableCell>Notification delivery</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code>sms-survey</code></TableCell>
      <TableCell>SMS survey metrics</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code>sms-notification</code></TableCell>
      <TableCell>SMS notification metrics</TableCell>
    </TableRow>
  </TableBody>
</Table>

## Accessibility

- Use `role="status"` on cards that update dynamically so assistive technology announces changes
- Include a descriptive `aria-label` on the card element — e.g. `"SMS Sent: 6,893, up 5.2% vs last week"`
- Color is never the sole indicator of trend direction — the arrow icon provides a redundant, shape-based signal
- Maintain a minimum **4.5:1** contrast ratio for all text values against the card background
- If cards are interactive (clickable), ensure they receive keyboard focus and display a visible focus ring

## Principles for use

<DosDonts>
  <Do description="Choose the icon that matches the metric category. Category recognition across a row of cards reduces cognitive load when scanning a dense dashboard." />
  <Dont description="Use Stats Cards for metrics that require context or drill-down. If the number needs explanation to be actionable, a table or detail panel is more appropriate." />
</DosDonts>

<DosDonts>
  <Do description="Use the correct trendType — increase for positive, decrease for negative. Consistent color coding trains users to scan by color without reading the text." />
  <Dont description="Omit the trend indicator unless the comparison period is genuinely unknown. A hidden trend removes the most actionable part of the card." />
</DosDonts>

## Code

### Basic usage

```tsx
import { StatCard } from '@/components/ui/stats-cards'

<StatCard
  title="SMS Sent"
  value="6,893"
  trend="5.2% vs last week"
  trendType="increase"
/>
```

### Dashboard row

```tsx
import { StatCard } from '@/components/ui/stats-cards'

<div style={{ display: 'flex', gap: 12, flexWrap: 'wrap' }}>
  <StatCard title="SMS Sent"      value="6,893"  trend="5.2% vs last week"  trendType="increase" icon="sms-sent" />
  <StatCard title="Delivery Rate" value="94.1%"  trend="1.3% vs last week"  trendType="decrease" icon="delivery-rate" />
  <StatCard title="Open Rate"     value="38.7%"  trend="2.0% vs last week"  trendType="increase" icon="open-rate" />
  <StatCard title="Response Rate" value="12.4%"  trend="0.8% vs last week"  trendType="neutral"  icon="response-rate" />
</div>
```

### Small size / gray `surface="blue"`

```tsx
<StatCard
  title="Opt-Out"
  value="142"
  trend="3.1% vs last week"
  trendType="decrease"
  icon="opt-out"
  size="small"
  surface="blue"
/>
```

### Without trend indicator

```tsx
<StatCard
  title="Voice Duration"
  value="4,210 min"
  showTrend={false}
  icon="voice-duration"
/>
```

## Open questions

- **7 of the 10 category icons were mapped to the wrong Phosphor icon entirely** — not a colour or size mismatch, a semantically different icon. Verified directly against Figma's "Metric Tile Icons" component (`814-10540`) per `iconLabel`: `voice-duration` → Microphone (was PhoneCall), `delivery-rate` → BoxArrowUp (was CheckCircle), `response-rate` → UploadSimple (was ChatCircle), `opt-out` → SubtitlesSlash (was UserMinus), `voice-survey` → UserSound (was Microphone), `voice-notification` → ListChecks (was BellSimple), `sms-notification` → ClipboardText (was ChatDots). Only `sms-sent`, `open-rate`, and `sms-survey` were already correct. All corrected.
- **Icon weight is unverified.** Figma's icon assets are flattened SVG exports with no readable Phosphor weight attribute. The previous code used `weight="fill"` (solid); switched to `weight="regular"` to match this design system's established line-icon convention used everywhere else (nav items, table actions, etc.) — an inferred, not Figma-confirmed, choice.
- **Resolved: the `surface="blue"` prop is intentionally retained for Figma/API compatibility, while its actual surface is `--surface-table-zebra-row` gray (`#f8f8f8`).**
