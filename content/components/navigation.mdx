---
title: Navigation
description: Vertical side navigation with collapsible groups, icon-labelled headers, and two-level hierarchy. Dark-mode-first with Light/SemiBold typography and green interactive states. Superseded by NT Menu as the live sidebar (2026-09-10) — kept as a component/doc, not deleted.
figmaNodeId: 8-2368
figmaUpdated: Apr 09, 2026
status: stable
---

## Overview

Navigation shows users where they are in the product and where they can go. The Design System vertical navigation supports a two-level hierarchy — group headers at the top level and sub-items nested below — providing access to multi-section application areas while keeping the overall structure compact and scannable.

> **Navigation goals:** Let the user know exactly where they are in the product and take them to where they want to go quickly and effortlessly.

## When to use

- When the application has **6 or more distinct destinations**
- When the structure has **1–3 levels of hierarchy** (prefer 2 for clarity)
- When side context — seeing the menu while viewing content — improves orientation
- When the same nav is shared across many different views and sections

**Do not use** the vertical nav as the primary navigation when the application has fewer than 6 pages. Prefer a top navigation bar for flat, few-page structures.

## Principles

### Discoverability
Users can locate the information they want. Design the information architecture to be as light-weight and inclusive as possible.

### Efficiency
- **Multiple access points** — multiple links to the same destination where helpful
- **Shortcuts** — provide shortcuts to content, such as related links
- **Escape pods** — return to the home page by clicking the logo so users can restart their journey

## Anatomy

- **Sidebar panel** — Fixed left panel, 240px expanded / 64px collapsed, dark background (`#050326`). Animates via framer-motion.
- **Brand header** — Logo mark + product name + sub-title. 64px tall. Collapse toggle (`<List />` icon) sits flush right.
- **Group header (Menu Item)** — 48px tall. Leading icon (18px, thin weight) + label + expand/collapse caret (14px). Toggles the sub-item list. In collapsed mode: 48×48px icon-only, centered.
- **Sub-items (Sub Menu Item)** — 48px tall. 36px left indent, 8px right padding. Label only (no icon). Status badge optional. Hidden entirely when sidebar is collapsed.
- **Collapse toggle** — `<List />` icon button (28×28px, 6px radius) in the brand header. Collapses/expands the full sidebar.

## Variants

### States

<Table size="compact" className="mb-6">
  <TableHeader>
    <tr>
      <TableHead>State</TableHead>
      <TableHead>Background</TableHead>
      <TableHead>Label weight</TableHead>
      <TableHead>Use for</TableHead>
    </tr>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell><strong>Default</strong></TableCell>
      <TableCell>Transparent</TableCell>
      <TableCell>Light (300)</TableCell>
      <TableCell>Resting, not interacted</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><strong>Hover</strong></TableCell>
      <TableCell><code>#366618</code></TableCell>
      <TableCell>Light (300)</TableCell>
      <TableCell>Mouse-over — preview of selection</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><strong>Active (group header)</strong></TableCell>
      <TableCell>Transparent — label color <code>#3a8015</code></TableCell>
      <TableCell>SemiBold (600)</TableCell>
      <TableCell>Open group header containing the active page</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><strong>Active (sub-item)</strong></TableCell>
      <TableCell><code>#3a8015</code></TableCell>
      <TableCell>SemiBold (600)</TableCell>
      <TableCell>Currently selected page link</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><strong>Disabled</strong></TableCell>
      <TableCell>Transparent</TableCell>
      <TableCell>Light (300)</TableCell>
      <TableCell>Feature not available; text <code>#adadad</code></TableCell>
    </TableRow>
  </TableBody>
</Table>

### Item types

<Table size="compact" className="mb-6">
  <TableHeader>
    <tr>
      <TableHead>Type</TableHead>
      <TableHead>Height</TableHead>
      <TableHead>Left padding</TableHead>
      <TableHead>Right padding</TableHead>
      <TableHead>Context</TableHead>
    </tr>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell><strong>Menu Item</strong> (group header)</TableCell>
      <TableCell>48px</TableCell>
      <TableCell>12px</TableCell>
      <TableCell>12px</TableCell>
      <TableCell>Top-level group, always has icon + caret</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><strong>Sub Menu Item</strong></TableCell>
      <TableCell>48px</TableCell>
      <TableCell>36px</TableCell>
      <TableCell>8px</TableCell>
      <TableCell>Child route, text only</TableCell>
    </TableRow>
  </TableBody>
</Table>

## Information Architecture

Two recommended approaches for organizing navigation groups:

1. **Categorize by topic** — group content directly related to the service or content scope of the site
2. **Categorize by tasks** — group by cooperation modalities, contacting cooperation specialists, signing processes, business operations, and customer service

The importance of each menu item is less affected by the arrangement in a side navigation than in a top bar — all items share equivalent visual weight until one is active.

> **Note:** It is not recommended to use distinct categories for color/products unless there are better visibility cues that support frequent use.

## Do's & Don'ts

<DosDonts>
  <Do description="Keep group labels short — one or two words that clearly identify the section. Users scan labels; they shouldn't need to read them carefully to understand the scope." />
  <Dont description="Use verb phrases like 'Manage Campaigns' or 'View Insights' for group headers. Noun phrases ('Campaigns', 'Insights') are faster to scan and match the user's mental model of sections, not tasks." />
</DosDonts>

<DosDonts>
  <Do description="Auto-expand the group that contains the current active page when the user first lands on or navigates to that route. It reduces orientation effort." />
  <Dont description="Expand all groups by default. Showing all sub-items at once defeats the purpose of collapsible groups and overwhelms the visual hierarchy." />
</DosDonts>

<DosDonts>
  <Do description="Limit sub-items per group to 5–8 items. If a group grows beyond that, consider splitting it into two groups or adding a second level of hierarchy." />
  <Dont description="Add more than 3 levels of nesting. The current component supports 2 levels (group + sub-item). Deep nesting creates orientation problems and makes the back-path unclear." />
</DosDonts>

<DosDonts>
  <Do description="Keep disabled nav items visible in the sidebar so users understand the product's full scope. Pair them with surrounding context or a tooltip explaining why the section is locked." />
  <Dont description="Remove disabled items from the nav. Users lose awareness of capabilities that exist but are not yet available to them — especially important in tiered products." />
</DosDonts>

<DosDonts>
  <Do description="Use the thin-weight icon style (18px, thin) for group header icons. This maintains visual lightness and keeps icons from competing with the label text." />
  <Dont description="Use filled or bold icons in the sidebar. They create too much visual weight and make the dark-background panel feel cluttered when multiple groups are visible." />
</DosDonts>

## Collapse animation

The sidebar collapses from 240px to 64px via a `framer-motion` width tween. All text labels fade out simultaneously; each group header shrinks to an icon-only 48×48px button.

### Timing

| Property | Value |
|---|---|
| Duration | 220ms |
| Easing | `[0.4, 0, 0.2, 1]` (Material standard) |
| Label fade | 180ms, same easing |
| Sub-list slide | 180ms height tween, `AnimatePresence` |

### Collapsed item (48×48)

In the collapsed state each group header becomes a centered icon button. The states match the expanded panel:

<Table size="compact" className="mb-6">
  <TableHeader>
    <tr>
      <TableHead>State</TableHead>
      <TableHead>Background</TableHead>
    </tr>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell>Default</TableCell>
      <TableCell>Transparent</TableCell>
    </TableRow>
    <TableRow>
      <TableCell>Hover</TableCell>
      <TableCell><code>#366618</code></TableCell>
    </TableRow>
    <TableRow>
      <TableCell>Active group</TableCell>
      <TableCell><code>#3a8015</code></TableCell>
    </TableRow>
    <TableRow>
      <TableCell>Disabled</TableCell>
      <TableCell>Transparent, icon muted</TableCell>
    </TableRow>
  </TableBody>
</Table>

### Behavior rules

- Clicking the `<List />` toggle collapses or expands the sidebar.
- When the sidebar collapses, all open sub-item lists close instantly.
- When the sidebar expands, it re-opens the group that contains the current active route.
- Only **one group can be open at a time** — opening a new group closes the previously open one.
- Sub-items are not accessible while collapsed (keyboard or pointer). This is intentional; the collapsed state is a space-saving mode, not an alternative navigation mode.

## Code

### NavMenuItem

```tsx
import { NavMenuItem } from '@/components/ui/nav-item'
import { ChartBarIcon } from '@phosphor-icons/react'

// Default
<NavMenuItem
  label="Campaigns"
  icon={<ChartBarIcon size={18} weight="thin" />}
/>

// Active + open (transparent bg, label turns #3a8015)
<NavMenuItem
  label="Campaigns"
  icon={<ChartBarIcon size={18} weight="thin" />}
  state="active"
  isOpen
/>

// Light mode (CxCentral)
<NavMenuItem
  label="Knowledge Management"
  icon={<BrainIcon size={18} weight="thin" />}
  state="active"
  isOpen
  darkMode={false}
/>

// Disabled
<NavMenuItem
  label="Campaigns"
  icon={<ChartBarIcon size={18} weight="thin" />}
  state="disabled"
/>
```

### NavSubItem

```tsx
import { NavSubItem } from '@/components/ui/nav-item'

<NavSubItem label="Queue Availability" />
<NavSubItem label="Transfer Points" state="active" />
<NavSubItem label="Change Requests"  state="disabled" />
```

### Full sidebar (layout component)

```tsx
import { Sidebar } from '@/components/layout/Sidebar'

// Place once in the root layout — Sidebar is position:fixed
<Sidebar />
```

### Collapse animation setup (framer-motion)

```tsx
import { motion, AnimatePresence } from 'framer-motion'

const EXPANDED_WIDTH  = 240
const COLLAPSED_WIDTH = 64

// Sidebar panel
<motion.aside
  animate={{ width: collapsed ? COLLAPSED_WIDTH : EXPANDED_WIDTH }}
  transition={{ duration: 0.22, ease: [0.4, 0, 0.2, 1] }}
>
  {/* Brand text — fade out on collapse */}
  <motion.div
    animate={{ opacity: collapsed ? 0 : 1, width: collapsed ? 0 : 'auto' }}
    transition={{ duration: 0.18, ease: [0.4, 0, 0.2, 1] }}
  >
    <p>CxPortal</p>
    <p>Design System</p>
  </motion.div>

  {/* Sub-item list — height tween with AnimatePresence */}
  <AnimatePresence initial={false}>
    {isOpen && (
      <motion.div
        initial={{ height: 0, opacity: 0 }}
        animate={{ height: 'auto', opacity: 1 }}
        exit={{ height: 0, opacity: 0 }}
        transition={{ duration: 0.18, ease: [0.4, 0, 0.2, 1] }}
        style={{ overflow: 'hidden' }}
      >
        {subItems}
      </motion.div>
    )}
  </AnimatePresence>
</motion.aside>
```

### Menu item (group header) — token reference

```tsx
// Default state
<div style={{
  height: 48,
  display: 'flex',
  alignItems: 'center',
  gap: 8,
  padding: '0 12px',
  background: 'transparent',
}}>
  <Icon size={18} weight="thin" color="#f8f8f8" />
  <span style={{ flex: 1, fontSize: 14, fontWeight: 300, color: '#f8f8f8' }}>
    Group Label
  </span>
  <CaretRightIcon size={14} />
</div>

// Active state (expanded group header — transparent bg, green text)
<div style={{
  height: 48,
  background: 'transparent',
  /* … */
}}>
  <span style={{ fontWeight: 600, color: '#3a8015' }}>Group Label</span>
</div>
```

### Sub-menu item — token reference

```tsx
// Default — full-bright text on the dark theme, not a muted/reduced-opacity one
<div style={{ height: 48, paddingLeft: 36, paddingRight: 8 }}>
  <span style={{ fontSize: 14, fontWeight: 300, color: '#f8f8f8' }}>
    Sub Item
  </span>
</div>

// Active
<div style={{ height: 48, paddingLeft: 36, paddingRight: 8, background: '#3a8015' }}>
  <span style={{ fontSize: 14, fontWeight: 600, color: '#f8f8f8' }}>
    Sub Item
  </span>
</div>
```

## Token reference

<Table size="compact" className="mb-6">
  <TableHeader>
    <tr>
      <TableHead>Token</TableHead>
      <TableHead>Value</TableHead>
      <TableHead>Usage</TableHead>
    </tr>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell><code>--surface-vertical-nav</code></TableCell>
      <TableCell><code>#050326</code></TableCell>
      <TableCell>Sidebar background</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code>--surface-action-primary-hover</code></TableCell>
      <TableCell><code>#366618</code></TableCell>
      <TableCell>Hover background — menu item, sub-item, and collapsed icon button</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code>--content-action-primary-default</code></TableCell>
      <TableCell><code>#3a8015</code></TableCell>
      <TableCell>Active sub-item background <em>and</em> active group-header label + caret colour</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code>--text-on-action-primary</code></TableCell>
      <TableCell><code>#f8f8f8</code></TableCell>
      <TableCell>Default and active label text</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code>--text-form-field-disabled</code></TableCell>
      <TableCell><code>#adadad</code></TableCell>
      <TableCell>Disabled label text</TableCell>
    </TableRow>
    <TableRow>
      <TableCell>Font weight Light</TableCell>
      <TableCell>300</TableCell>
      <TableCell>Default items</TableCell>
    </TableRow>
    <TableRow>
      <TableCell>Font weight SemiBold</TableCell>
      <TableCell>600</TableCell>
      <TableCell>Active sub-items</TableCell>
    </TableRow>
  </TableBody>
</Table>

### Light mode (CxCentral)

Pass `darkMode={false}` on both `NavMenuItem` and `NavSubItem` when rendering inside a CxCentral product shell (white sidebar background). Hover/active
fills and text are identical to the dark theme — only idle (default) text differs.

<Table size="compact" className="mb-6">
  <TableHeader>
    <tr>
      <TableHead>State</TableHead>
      <TableHead>Background</TableHead>
      <TableHead>Text color</TableHead>
    </tr>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell>Group header — Default</TableCell>
      <TableCell>Transparent</TableCell>
      <TableCell><code>#1d1d1d</code></TableCell>
    </TableRow>
    <TableRow>
      <TableCell>Group header — Hover</TableCell>
      <TableCell><code>#366618</code></TableCell>
      <TableCell><code>#1d1d1d</code></TableCell>
    </TableRow>
    <TableRow>
      <TableCell>Group header — Active</TableCell>
      <TableCell>Transparent</TableCell>
      <TableCell><code>#3a8015</code></TableCell>
    </TableRow>
    <TableRow>
      <TableCell>Sub-item — Default</TableCell>
      <TableCell><code>#fbfcf2</code> (tinted)</TableCell>
      <TableCell><code>#1d1d1d</code></TableCell>
    </TableRow>
    <TableRow>
      <TableCell>Sub-item — Hover</TableCell>
      <TableCell><code>#366618</code></TableCell>
      <TableCell><code>#1d1d1d</code></TableCell>
    </TableRow>
    <TableRow>
      <TableCell>Sub-item — Active</TableCell>
      <TableCell><code>#3a8015</code></TableCell>
      <TableCell><code>#f8f8f8</code> semibold</TableCell>
    </TableRow>
  </TableBody>
</Table>

## Accessibility

- The `<aside>` landmark element communicates the sidebar's role to screen readers.
- Group headers are rendered as `<button>` elements so they receive keyboard focus and `Enter`/`Space` toggles the group.
- Sub-item links are native `<a>` elements for full keyboard navigation.
- The active item reflects the current page — combine with `aria-current="page"` on the active link for screen-reader announcements.
- The collapsed/expanded state of group headers is communicated with `aria-expanded` on the button.
- The collapse toggle carries `aria-label="Collapse navigation"` / `aria-label="Expand navigation"` so its purpose is announced when focused.
- When the sidebar is collapsed, sub-items are removed from the DOM (`AnimatePresence` exit) and therefore unreachable by keyboard — this matches the visual affordance.
- Avoid relying on colour alone to communicate the active state — the SemiBold font weight provides a second visual cue.

## Open questions — not resolved

- ~~**This pattern is being redesigned.**~~ **Resolved (2026-09-10)** — the
  light-themed, rounded-pill **NT Menu** pattern is now the live sidebar
  (`Sidebar.tsx`). This dark pattern's components (`NavMenuItem`/`NavSubItem`/
  `NavMenuItemCollapsed`) are no longer consumed anywhere in the app — kept
  as a standalone component + doc page for reference, not deleted.
- **Sub-item height (48px, not 40px) came from a 2-vs-1 evidence split.**
  The standalone Vertical Nav Item Expanded component (`3016-19265`) and the
  Nav Groups compound example (`203-14455`) both show 48px rows with 36px/8px
  padding; only this page's own hand-drawn "Variants" comparison grid (in the
  Usage doc, `735-30339`) still shows the older 40px/48px/24px numbers. Went
  with the two live component sources over the doc's static comparison grid.
- ~~**`Sidebar.tsx` doesn't consume `NavMenuItem`/`NavSubItem`/`NavMenuItemCollapsed`
  at all.**~~ **Resolved (2026-09-10) — superseded, not fixed.** `Sidebar.tsx`
  now consumes NT Menu's components instead (see above), rather than being
  refactored to finally consume this dark pattern's components. The
  duplication is gone because this pattern is no longer the live one, not
  because the two were unified.
