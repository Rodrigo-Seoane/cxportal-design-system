---
title: Tabs
description: Compact tab strip for switching between sibling views. Supports 2–5 tabs, optional icons, disabled states, and full keyboard navigation.
figmaNodeId: 280-20700
figmaUpdated: Apr 09, 2026
status: stable
---

## Overview

Tabs organise sibling content into a compact, switchable strip. They let users navigate between related views without leaving the page context — making them ideal for filtering lists, toggling between display modes, or stepping through sections inside a panel or modal.

The component supports two visual types. **Button** (default) wraps tabs in a rounded pill container with a grey background; the active tab has a white card with a blue border. **Minimal** renders tabs inline with no container — the active tab is distinguished by a blue bottom border underline, ideal for embedded contexts like trays, cards, or side panels.

## When to use

- When the page or panel has **2–5 distinct sibling views** that share the same layout space
- When switching between views should feel **instant and in-context** (no page load)
- When users benefit from seeing all available views upfront — tabs expose all options at once
- As a **filter strip** above a list or table (e.g. All / Active / Inactive)
- As a **view-mode switcher** (e.g. List View / Grid View)
- Inside a **modal or drawer** to switch between sections without extra navigation

**Do not use** tabs when:
- There are more than 5 tabs — prefer a secondary navigation or a Select dropdown
- The views are sequential steps that must be completed in order — use a Stepper instead
- The content areas are not closely related — consider separate pages or sections
- Only one tab would ever be available — show the content directly, skip the tabs

## Anatomy

- **Tab group (`TabList`)** — The outer container. Button type: `#efefef` background, 4px border-radius, 4px padding, 4px gap between tabs. Minimal type: transparent, no padding or radius, 4px horizontal padding only, **12px** gap between tabs (not 4px — items are pinned to the bottom edge so the underline sits flush).
- **Tab trigger (`Tab`)** — 10px SemiBold label, optional icon on the left (**12px for Button type, 9px for Minimal** — not a universal 16px), 4px vertical padding. Button type: 12px horizontal padding, 8px icon-label gap, 4px border-radius. Minimal type: **2px horizontal / 4px vertical** padding (not uniform 4px), 4px icon-label gap, 24px min-height, no radius.
- **Tab panel (`TabPanel`)** — The content region associated with the active tab. Rendered below the tab strip; hidden when its tab is not active.

## Variants

### Types

<Table size="compact" className="mb-6">
  <TableHeader>
    <tr>
      <TableHead>Type</TableHead>
      <TableHead>Container</TableHead>
      <TableHead>Active indicator</TableHead>
      <TableHead>Best for</TableHead>
    </tr>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell><strong>Button</strong> (default)</TableCell>
      <TableCell><code>#efefef</code> pill, 4px radius + padding</TableCell>
      <TableCell>White card + <code>1px #629944</code> border</TableCell>
      <TableCell>Standalone groups — page filters, view-mode switchers</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><strong>Minimal</strong></TableCell>
      <TableCell>Transparent, no padding</TableCell>
      <TableCell>Green <code>1px</code> bottom border underline</TableCell>
      <TableCell>Embedded groups — trays, cards, side panels, modals</TableCell>
    </TableRow>
  </TableBody>
</Table>

### States

<Table size="compact" className="mb-6">
  <TableHeader>
    <tr>
      <TableHead>State</TableHead>
      <TableHead>Button bg</TableHead>
      <TableHead>Minimal indicator</TableHead>
      <TableHead>Text colour</TableHead>
      <TableHead>Description</TableHead>
    </tr>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell><strong>Active</strong> (Figma calls this state "Focus" — it means selected, not keyboard focus)</TableCell>
      <TableCell><code>#ffffff</code> + <code>1px #629944</code></TableCell>
      <TableCell>Bottom <code>1px #629944</code></TableCell>
      <TableCell><code>#3a8015</code></TableCell>
      <TableCell>Currently selected view</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><strong>Default</strong></TableCell>
      <TableCell>Transparent</TableCell>
      <TableCell>None</TableCell>
      <TableCell><code>#1d1d1d</code></TableCell>
      <TableCell>Idle, available to select</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><strong>Hover</strong></TableCell>
      <TableCell>Solid <code>#ffffff</code>, no border</TableCell>
      <TableCell>Solid <code>#ffffff</code>, no underline</TableCell>
      <TableCell><code>#3a8015</code> — Figma previews the active look on hover, same green as Active</TableCell>
      <TableCell>Mouse over an idle tab</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><strong>Disabled</strong></TableCell>
      <TableCell>Transparent</TableCell>
      <TableCell>None</TableCell>
      <TableCell><code>#adadad</code></TableCell>
      <TableCell>Feature not yet available</TableCell>
    </TableRow>
  </TableBody>
</Table>

### Icon support

Each tab optionally accepts a 16px icon to the left of the label. Icons help disambiguate view modes at a glance (e.g. a table icon for List View, a grid icon for Grid View). Keep icon weight consistent across all tabs in a group — thin or regular, not mixed.

<Table size="compact" className="mb-6">
  <TableHeader>
    <tr>
      <TableHead>Variant</TableHead>
      <TableHead>Icon</TableHead>
      <TableHead>Best for</TableHead>
    </tr>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell><strong>Text only</strong></TableCell>
      <TableCell>—</TableCell>
      <TableCell>Filter strips, status groupings (All / Active / Inactive)</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><strong>Icon + label</strong></TableCell>
      <TableCell>12px (Button type) / 9px (Minimal type), left of label</TableCell>
      <TableCell>View-mode switchers where the icon quickly communicates the layout type</TableCell>
    </TableRow>
  </TableBody>
</Table>

## Usage in context

The Tabs component appears across the product in three main patterns:

**Filter strip above a table** — The most common usage. Placed between the page heading and the data table, tabs like "All Users", "Active", "Inactive" filter the rows below without a page reload. Usually paired with a search input and action buttons on the same row.

**View-mode switcher** — Used to toggle between data representations: List View vs. Grid View, for example. Tabs with icons communicate the display format at a glance. Typically placed at the top-right of a content panel.

**Modal section switcher** — Inside a modal or drawer with multiple related sections, tabs replace scrolling or accordion patterns and let users jump directly to the section they need. Used when sections share similar structure (e.g. "Updating existing proficiency" and "Adding new proficiency" within a bulk-change modal).

## Do's & Don'ts

<DosDonts>
  <Do description="Keep all tab labels short — one to three words. The strip should be scannable at a glance. If labels are getting long, reconsider whether tabs are the right pattern." />
  <Dont description="Use sentence-length labels or descriptions inside tabs. Tab labels are navigation anchors, not headings — the content area is where descriptions belong." />
</DosDonts>

<DosDonts>
  <Do description="Always make one tab active by default. Provide a defaultValue (uncontrolled) or a controlled value prop. Never show a tab group with no active tab." />
  <Dont description="Leave all tabs in the default (idle) state on initial render. Users need immediate orientation about which view is active." />
</DosDonts>

<DosDonts>
  <Do description="Keep disabled tabs visible in the strip so users understand what views exist even if they are not yet accessible. Pair with a tooltip explaining why the tab is locked." />
  <Dont description="Remove disabled tabs from the DOM. Users lose awareness of features that exist but are not yet available to them." />
</DosDonts>

<DosDonts>
  <Do description="Use tabs for sibling views that are truly parallel — same type of content, same user goal. The user should feel they are looking at different facets of the same thing." />
  <Dont description="Use tabs as a substitute for a stepper or wizard. If the order matters or a step must be completed before the next is available, use a Stepper component instead." />
</DosDonts>

<DosDonts>
  <Do description="Limit to 5 tabs maximum. The strip is designed for 2–5 tabs at the widths used in the product. More tabs break the compact layout and overwhelm the user." />
  <Dont description="Exceed 5 tabs. If the information architecture requires more sections, use the vertical Navigation or a secondary Select dropdown to expose the options." />
</DosDonts>

## Code

### Basic (uncontrolled)

```tsx
import { Tabs, TabList, Tab, TabPanel } from '@/components/ui/tabs'

<Tabs defaultValue="all">
  <TabList aria-label="Filter users">
    <Tab value="all">All Users</Tab>
    <Tab value="active">Active</Tab>
    <Tab value="inactive">Inactive</Tab>
  </TabList>

  <TabPanel value="all">
    {/* All users table */}
  </TabPanel>
  <TabPanel value="active">
    {/* Active users table */}
  </TabPanel>
  <TabPanel value="inactive">
    {/* Inactive users table */}
  </TabPanel>
</Tabs>
```

### With icons

```tsx
import { TableIcon, RowsIcon } from '@phosphor-icons/react'

{/* Button type: 12px icons */}
<Tabs defaultValue="list">
  <TabList aria-label="View mode">
    <Tab value="list" icon={<TableIcon size={12} weight="regular" />}>
      List View
    </Tab>
    <Tab value="grid" icon={<RowsIcon size={12} weight="regular" />}>
      Grid View
    </Tab>
  </TabList>
</Tabs>

{/* Minimal type: 9px icons */}
<Tabs defaultValue="list" type="minimal">
  <TabList aria-label="View mode">
    <Tab value="list" icon={<TableIcon size={9} weight="regular" />}>
      List View
    </Tab>
    <Tab value="grid" icon={<RowsIcon size={9} weight="regular" />}>
      Grid View
    </Tab>
  </TabList>
</Tabs>
```

### Controlled

```tsx
const [view, setView] = useState('all')

<Tabs value={view} onChange={setView}>
  <TabList aria-label="Filter users">
    <Tab value="all">All Users</Tab>
    <Tab value="active">Active</Tab>
    <Tab value="inactive">Inactive</Tab>
  </TabList>
</Tabs>
```

### With a disabled tab

```tsx
<Tabs defaultValue="all">
  <TabList aria-label="Filter content">
    <Tab value="all">All</Tab>
    <Tab value="active">Active</Tab>
    <Tab value="archived" disabled>Archived</Tab>
  </TabList>
</Tabs>
```

### Minimal type

```tsx
<Tabs defaultValue="list" type="minimal">
  <TabList aria-label="View mode">
    <Tab value="list">List View</Tab>
    <Tab value="grid">Grid View</Tab>
  </TabList>
</Tabs>
```

### Minimal with disabled

```tsx
<Tabs defaultValue="active" type="minimal">
  <TabList aria-label="Filter">
    <Tab value="active">Active</Tab>
    <Tab value="draft">Draft</Tab>
    <Tab value="archived" disabled>Archived</Tab>
  </TabList>
</Tabs>
```

### Inline with other controls

```tsx
{/* Common pattern: tabs + search + count on a single toolbar row */}
<div style={{ display: 'flex', alignItems: 'center', gap: 16 }}>
  <Tabs defaultValue="all">
    <TabList aria-label="Filter agents">
      <Tab value="all">All</Tab>
      <Tab value="updating">Updating</Tab>
      <Tab value="adding">Adding</Tab>
    </TabList>
  </Tabs>
  <Input variant="text" labelVisible={false} placeholder="Search agents" />
  <span style={{ marginLeft: 'auto', fontSize: 14, color: '#8d8d8d' }}>
    Showing 9 of 9 agents
  </span>
</div>
```

## Token reference

<Table size="compact" className="mb-6">
  <TableHeader>
    <tr>
      <TableHead>Token</TableHead>
      <TableHead>Value</TableHead>
      <TableHead>Usage</TableHead>
    </tr>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell><code>--surface/main-panel</code></TableCell>
      <TableCell><code>#efefef</code></TableCell>
      <TableCell>Tab group container background</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code>--surface/form-field</code></TableCell>
      <TableCell><code>#ffffff</code></TableCell>
      <TableCell>Active tab background</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code>--border-color-surface-active-primary-default</code></TableCell>
      <TableCell><code>#629944</code></TableCell>
      <TableCell>Active tab border (Button) / underline (Minimal) — note this is a different, lighter green than the label colour below</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code>--content-action-primary-default</code></TableCell>
      <TableCell><code>#3a8015</code></TableCell>
      <TableCell>Active <em>and</em> hover tab label/icon colour. Applied directly rather than via the shared <code>--text-action</code> alias, which resolves to the wrong ramp step (<code>#204704</code>) in this codebase — see <strong>Open questions</strong>.</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code>--neutral-800</code></TableCell>
      <TableCell><code>#1d1d1d</code></TableCell>
      <TableCell>Default (idle) tab label colour. Figma names this <code>--text/on-action/secondary</code>, but that shared alias also resolves to the wrong value (<code>#373737</code>) here — same reason as above.</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code>--text-form-field-disabled</code></TableCell>
      <TableCell><code>#adadad</code></TableCell>
      <TableCell>Disabled tab label colour</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code>--surface-main-panel</code></TableCell>
      <TableCell><code>#efefef</code></TableCell>
      <TableCell>Button-type group background</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code>--surface-form-field</code></TableCell>
      <TableCell><code>#ffffff</code></TableCell>
      <TableCell>Active and hover tab background (Button type)</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code>--border-radius/sm</code></TableCell>
      <TableCell>4px</TableCell>
      <TableCell>Border radius for both group and individual tabs (Button type)</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code>--scale/1</code></TableCell>
      <TableCell>4px</TableCell>
      <TableCell>Button-type group padding, tab vertical padding, and Minimal-type tab vertical padding</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code>--scale/3</code></TableCell>
      <TableCell>12px</TableCell>
      <TableCell>Button-type tab horizontal padding</TableCell>
    </TableRow>
    <TableRow>
      <TableCell>Minimal group gap</TableCell>
      <TableCell>12px</TableCell>
      <TableCell>Between tabs in a Minimal-type <code>TabList</code> — not the same 4px used everywhere else</TableCell>
    </TableRow>
    <TableRow>
      <TableCell>Minimal tab padding</TableCell>
      <TableCell>2px horizontal / 4px vertical</TableCell>
      <TableCell>Not uniform 4px</TableCell>
    </TableRow>
    <TableRow>
      <TableCell>Minimal tab gap</TableCell>
      <TableCell>4px</TableCell>
      <TableCell>Between icon and label (Button type uses 8px)</TableCell>
    </TableRow>
    <TableRow>
      <TableCell>Minimal min-height</TableCell>
      <TableCell>24px</TableCell>
      <TableCell>Both the group and each tab trigger</TableCell>
    </TableRow>
    <TableRow>
      <TableCell>Icon size</TableCell>
      <TableCell>12px (Button) / 9px (Minimal)</TableCell>
      <TableCell>Not a universal 16px</TableCell>
    </TableRow>
    <TableRow>
      <TableCell>Font size</TableCell>
      <TableCell>10px</TableCell>
      <TableCell>Tab label — Caption/regular</TableCell>
    </TableRow>
    <TableRow>
      <TableCell>Font weight</TableCell>
      <TableCell>600 (SemiBold)</TableCell>
      <TableCell>Tab label weight</TableCell>
    </TableRow>
    <TableRow>
      <TableCell>Letter spacing</TableCell>
      <TableCell>0.4px</TableCell>
      <TableCell>Tab label tracking</TableCell>
    </TableRow>
  </TableBody>
</Table>

### Open questions — Figma is internally inconsistent here, not resolved

- **Tab count contradiction.** Principles (`2544-75780`) explicitly says "Tab groups support 2, 3, or 4 tabs. Do not exceed 4" — repeated in its own When to Use / When Not to Use sections. But the Tab Group component itself (`280-20700`) ships a labelled `Size=05` variant with 5 tabs, screenshot-rendered and everything. Per this design system's audit convention the component wins over prose docs — this page and the component now support 2-5. The Principles text is stale and needs a designer fix, not a silent doc pick.
- **Principles' colour values are pre-rebrand.** Its States section describes Active text as `#4285f4` (blue), Default text as `#021920`, and Disabled text as `#aab0b8` — none of which match the component, any current token, or anything else in this codebase. Not followed; the component's green values (`#3a8015` / `#629944` / `#1d1d1d` / `#adadad`) are what's built.
- **Two shared token aliases are themselves wrong-ramp-stepped.** `--text-action` (Figma: `text/action`) resolves to `#204704` here instead of Figma's `#3a8015`, and `--text-on-action-secondary` (Figma: `text/on-action/secondary`) resolves to `#373737` instead of `#1d1d1d` — the same recurring bug found on Modal, Toast, and the G1 Checkbox & Radio row. Not fixed globally: `--text-action` alone is also consumed by `table.tsx`, `Sidebar.tsx`, and several un-audited `open-inventory` components. Tabs bypasses both aliases locally (`--content-action-primary-default`, `--neutral-800` directly) rather than repointing the shared tokens.
- **Minimal active border-bottom width (1px) is inferred, not stated.** The component's Tailwind class is a bare `border-b` with no width modifier, which defaults to 1px — there's no explicit numeric spec to check it against. Moderate confidence, not pixel-ironclad.

## Accessibility

- `TabList` renders a `role="tablist"` container. Each `Tab` renders a `role="tab"` button. Each `TabPanel` renders `role="tabpanel"`.
- `aria-selected` is set to `true` on the active tab and `false` on all others — screen readers announce which tab is current.
- `aria-controls` on each tab and `aria-labelledby` on each panel link the trigger to its content region by ID.
- A roving `tabIndex` is used: only the active tab has `tabIndex={0}`, all others have `tabIndex={-1}`. Users reach the tab group with a single `Tab` key press.
- Once focused, `ArrowLeft` / `ArrowRight` move between tabs. `Home` jumps to the first tab; `End` jumps to the last.
- `Space` or `Enter` activates the focused tab.
- Disabled tabs carry the native `disabled` attribute — they are skipped by keyboard navigation and announced as unavailable by screen readers.
- Always provide `aria-label` on `TabList` when multiple tab groups exist on the same page so screen readers can distinguish between them.
