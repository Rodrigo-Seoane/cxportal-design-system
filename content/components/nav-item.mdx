---
title: Nav Item
description: Primitive components for vertical sidebar navigation — group header (NavMenuItem), child route link (NavSubItem), and collapsed icon button (NavMenuItemCollapsed). Supports dark and light product themes.
figmaNodeId: 8-2388
figmaUpdated: Apr 09, 2026
status: stable
---

## Overview

The Nav Item primitives are the building blocks of the sidebar navigation. They are consumed by the `Sidebar` layout component but can also be composed independently when building custom navigation contexts.

There are three exported components:

- **`NavMenuItem`** — 48px group header with leading icon, label, and expand/collapse caret
- **`NavSubItem`** — 48px child route link with left indent, text only
- **`NavMenuItemCollapsed`** — 48×48px icon-only button for the collapsed sidebar state

## When to use

Use these primitives when:
- Building a custom sidebar that differs from the default `Sidebar` layout component
- Rendering nav items in a Storybook story or design reference context
- Extending the navigation with additional item types that follow the same visual language

**Do not use** them to build page-level tabs or breadcrumbs — they are sidebar-specific components.

## Anatomy

### NavMenuItem

- **Leading icon** — 18px, thin weight Phosphor icon. Color inherits from the item's current state.
- **Label** — 14px, Light (300) by default, SemiBold (600) when active.
- **Trailing caret** — `CaretRightIcon` (closed) or `CaretDownIcon` (open), 14px. Color matches label.

### NavSubItem

- **Label** — 14px, 36px left indent, 8px right padding. No icon. Full-bright text at every state on the dark theme — light theme is the only case where idle/hover text is dark instead.

### NavMenuItemCollapsed

- 48×48px square. Icon centered. No label, no caret. Background changes per state.

## Variants

### States

<Table size="compact" className="mb-6">
  <TableHeader>
    <tr>
      <TableHead>State</TableHead>
      <TableHead>NavMenuItem bg</TableHead>
      <TableHead>NavMenuItem text</TableHead>
      <TableHead>NavSubItem bg</TableHead>
    </tr>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell><strong>Default</strong></TableCell>
      <TableCell>Transparent</TableCell>
      <TableCell><code>#f8f8f8</code></TableCell>
      <TableCell>Transparent</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><strong>Hover</strong></TableCell>
      <TableCell><code>#366618</code></TableCell>
      <TableCell><code>#f8f8f8</code></TableCell>
      <TableCell><code>#366618</code></TableCell>
    </TableRow>
    <TableRow>
      <TableCell><strong>Active</strong></TableCell>
      <TableCell>Transparent</TableCell>
      <TableCell><code>#3a8015</code> (green text)</TableCell>
      <TableCell><code>#3a8015</code></TableCell>
    </TableRow>
    <TableRow>
      <TableCell><strong>Disabled</strong></TableCell>
      <TableCell>Transparent</TableCell>
      <TableCell><code>#adadad</code></TableCell>
      <TableCell>Transparent</TableCell>
    </TableRow>
  </TableBody>
</Table>

### Product themes

Both `NavMenuItem` and `NavSubItem` accept a `darkMode` prop (default `true`).
Hover/active fills and text are identical across themes — only the idle
(default) text colour differs.

<Table size="compact" className="mb-6">
  <TableHeader>
    <tr>
      <TableHead>Prop</TableHead>
      <TableHead>Sidebar bg</TableHead>
      <TableHead>Hover</TableHead>
      <TableHead>Active accent</TableHead>
    </tr>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell><code>darkMode={'{true}'}</code> (default)</TableCell>
      <TableCell><code>#050326</code></TableCell>
      <TableCell><code>#366618</code></TableCell>
      <TableCell><code>#3a8015</code></TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code>darkMode={'{false}'}</code></TableCell>
      <TableCell><code>#ffffff</code></TableCell>
      <TableCell><code>#366618</code></TableCell>
      <TableCell><code>#3a8015</code></TableCell>
    </TableRow>
  </TableBody>
</Table>

## Do's & Don'ts

<DosDonts>
  <Do description="Pass state as a prop for static/story use. In the live Sidebar, hover state is derived from React's onMouseEnter/Leave and active from the router pathname — don't drive state from external logic." />
  <Dont description="Use NavMenuItem as a link element. It is a button that toggles a sub-list. Route navigation is handled by NavSubItem links." />
</DosDonts>

<DosDonts>
  <Do description="Use thin-weight Phosphor icons at 18px for NavMenuItem. This is the established visual standard across all CxPortal module icons." />
  <Dont description="Mix icon weights. Filled or bold icons compete with the label text and make the sidebar panel feel heavy." />
</DosDonts>

<DosDonts>
  <Do description="Render NavMenuItemCollapsed only when the sidebar is in its collapsed (64px) state. It replaces NavMenuItem 1:1 and shares the same icon prop." />
  <Dont description="Show NavMenuItemCollapsed alongside visible NavMenuItems. The two forms are mutually exclusive based on the sidebar width state." />
</DosDonts>

## Code

### NavMenuItem

```tsx
import { NavMenuItem } from '@/components/ui/nav-item'
import { BrainIcon } from '@phosphor-icons/react'

// Default
<NavMenuItem
  label="Knowledge Management"
  icon={<BrainIcon size={18} weight="thin" />}
/>

// Active + open (transparent bg, label turns #3a8015)
<NavMenuItem
  label="Knowledge Management"
  icon={<BrainIcon size={18} weight="thin" />}
  state="active"
  isOpen
/>

// Light mode (CxCentral)
<NavMenuItem
  label="Knowledge Management"
  icon={<BrainIcon size={18} weight="thin" />}
  state="active"
  isOpen
  darkMode={false}
/>
```

### NavSubItem

```tsx
import { NavSubItem } from '@/components/ui/nav-item'

<NavSubItem label="Knowledge Bases" />
<NavSubItem label="Add New Document" state="active" />
<NavSubItem label="Folders"          state="disabled" />

// Light mode
<NavSubItem label="Knowledge Bases" darkMode={false} />
```

### NavMenuItemCollapsed

```tsx
import { NavMenuItemCollapsed } from '@/components/ui/nav-item'
import { BrainIcon } from '@phosphor-icons/react'

// Dark (default)
<NavMenuItemCollapsed
  icon={<BrainIcon size={18} weight="thin" />}
  state="active"
/>

// Light
<NavMenuItemCollapsed
  icon={<BrainIcon size={18} weight="thin" />}
  state="active"
  darkMode={false}
/>
```

### Full group (dark)

```tsx
<NavMenuItem
  label="Knowledge Management"
  icon={<BrainIcon size={18} weight="thin" />}
  state="active"
  isOpen
/>
<NavSubItem label="Knowledge Bases"  state="active"  />
<NavSubItem label="Add New Document" state="default" />
<NavSubItem label="Folders"          state="default" />
<NavSubItem label="Tags"             state="default" />
```

## Token reference

<Table size="compact" className="mb-6">
  <TableHeader>
    <tr>
      <TableHead>Token</TableHead>
      <TableHead>Value</TableHead>
      <TableHead>Usage</TableHead>
    </tr>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell><code>--content-action-primary-default</code></TableCell>
      <TableCell><code>#3a8015</code></TableCell>
      <TableCell>Active group-header text + active sub-item bg (both themes)</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code>--surface-action-primary-hover</code></TableCell>
      <TableCell><code>#366618</code></TableCell>
      <TableCell>Hover bg — menu item, sub-item, and collapsed icon button (both themes)</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code>--text-on-action-primary</code></TableCell>
      <TableCell><code>#f8f8f8</code></TableCell>
      <TableCell>Default dark-theme label + all active/hover labels (both themes)</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code>--neutral-800</code></TableCell>
      <TableCell><code>#1d1d1d</code></TableCell>
      <TableCell>Light-theme (CxCentral) idle/hover label text. Figma names this <code>text/on-action/secondary</code>, but that shared alias resolves to the wrong value in this codebase — bypassed directly.</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code>--text-form-field-disabled</code></TableCell>
      <TableCell><code>#adadad</code></TableCell>
      <TableCell>Disabled state text and icon</TableCell>
    </TableRow>
  </TableBody>
</Table>

## Accessibility

- `NavMenuItem` renders as a `<button>` — keyboard focusable, toggled with Enter or Space.
- `NavSubItem` should be wrapped in a `<Link>` or `<a>` when used for real route navigation (the `Sidebar` component handles this automatically).
- `NavMenuItemCollapsed` renders as a `<button>` and should receive an `aria-label` equal to the group name when used without a visible label.
- The `disabled` state sets the HTML `disabled` attribute, removing the element from the tab order.
- Icon color changes communicate state — ensure the parent container also provides a non-color cue (font weight change) when distinguishing active from default.
